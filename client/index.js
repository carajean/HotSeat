import React from 'react';
import ReactDOM from 'react-dom';
import {Component} from 'react';

let oldNames = ['Riley', 'Roo'];
let pastNames = [];

function randomNum() {
    return Math.round(Math.random() * oldNames.length);
}




